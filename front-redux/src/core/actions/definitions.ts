const prefix = '[GLOBAL]';

export const actionIds = {
  SET_USER_INFORMATION: `${prefix}[0] Sets the nickname and isMaster of the user.`,
  SET_ROOM: '[1] sets the room where the user is going to be connected',
}
